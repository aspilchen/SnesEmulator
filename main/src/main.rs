// use memory::Memory;
// use state::State;
// mod ricoh5a22;
// use ricoh5a22;

use hardware::{state};

fn main() {
    // let bin = vec![0xA9, 10, 0, 0x85, 0, 0x65, 0];
    // let mut cpu = state::Ricoh5A22::default();
    // cpu.load(bin);

    // let op = cpu.fetch();
    // cpu.execute(&op);
    // println!("{}\n", cpu);
    
    // let op = cpu.fetch();
    // cpu.execute(&op);
    // println!("{}\n", cpu);

    // let op = cpu.fetch();
    // cpu.execute(&op);
    // println!("{}\n", cpu);
}
